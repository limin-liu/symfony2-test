{% extends 'base.html.twig' %}

{% block body %}
 <div class="container">  
    <h2 class="page-header">Detail</h2> 
    <table class="table table-striped">       
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ carnet.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ carnet.title }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ carnet.address }}</td>
            </tr>
            
            <tr>
                <th>Username</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>PhoneNo.</th>
                <td>{{ user.phone }}</td>
            </tr>
            <tr>
                <th>website</th>
                <td>{{ user.website }}</td>
            </tr>
            
       
            <tr>
                <td style='float:right;'>
                    <a href="{{ path('carnet_edit', { 'id': carnet.id })}}" class='btn btn-warning'>Edit</a>
                </td>
                <td>
                    {{ form_start(delete_form) }}
                        <input type="submit" value="Delete" class='btn btn-danger'>
                    {{ form_end(delete_form) }}
                </td>
           </tr>
        </tbody>
    </table>
    
    <ul>              
        <p><li>
            <a href="{{ path('carnet_index') }}">Back to the list</a>
        </li></p>
    </ul>
{% endblock %}
